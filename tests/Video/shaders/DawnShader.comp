layout(local_size_x = 1, local_size_y = 1, local_size_z = 1 ) in;

vec3 ApplyLights(float metallic) {
    vec3 Lo = vec3(0.0);
    return Lo;
}

void main() {
    vec2 roughnessMetallic = vec2(1.0, 1.0);
    float param = roughnessMetallic.r;
    vec3 color = ApplyLights(param);
}
