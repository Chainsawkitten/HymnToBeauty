#pragma once

/// A scene containing entities.
class Scene {
    public:
        /// Create a new scene.
        Scene();
        
        /// Destructor.
        ~Scene();
        
        /// Clear the scene of all entities.
        void Clear();
};
